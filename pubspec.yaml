name: esp_local_control
description: A starting point for Dart libraries or applications.
version: 1.0.0
homepage: https://www.example.com

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  multicast_dns: ^0.2.2
  protobuf: ^1.1.0
  meta: ^1.2.4

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.4
